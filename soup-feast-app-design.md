# The Great Soup Feast — Product Requirements Document

> A standalone web app for organizing, voting on, and celebrating an annual soup competition. ~100 guests, ~25 soups, year 3 of the tradition.

---

## 1. Core Concepts

| Concept | Description |
|---|---|
| **Event** | A single Soup Feast (annual). One active event at a time, with an archive of past years. |
| **Soup** | A competition entry — name, cook, description, dietary tags, crockpot number. |
| **Guest** | An attendee. Identified by a unique link token — no account creation. |
| **Cook** | A guest who registered a soup. One soup per cook (or team). |
| **Vote** | A ranked-choice ballot. Each guest ranks their top 5 soups. |
| **Campaign** | A timed drip of messages (email + SMS via Twilio) leading up to the event. |

---

## 2. User Roles

| Role | Access |
|---|---|
| **Admin** | Everything — event setup, soup management, guest list, campaigns, budget, results reveal. Password-protected. |
| **Cook** | Sign up a soup, edit their entry, view leaderboard. Gets a unique cook link on registration. |
| **Guest** | RSVP, vote, view leaderboard, optionally leave tasting notes. Gets a unique guest link via invite. |

**No login walls.** Guests and cooks are identified by unique link tokens. Admin uses a simple password.

---

## 3. Decisions (Resolved)

These were open questions — now locked in:

| # | Decision | Resolution |
|---|---|---|
| 1 | **Guest identification** | Unique links per guest, auto-generated by the app. Admin just manages the guest list — no manual link wrangling. Postcard uses a shared QR code for RSVP; app handles individual tracking from there. |
| 2 | **Postcard design** | App exports event content + QR code. Jay designs the final postcard in Canva. |
| 3 | **Historical data** | Year 3. App includes an archive section. Years 1 & 2 can be backfilled with winner, runner-up, soup list, and any available photos. |
| 4 | **SMS provider** | Twilio. ~$5 total for 100 guests x 6 messages. $1.15/mo for the number. Worth it. |
| 5 | **Tasting notes privacy** | Default to private. Cooks receive a comprehensive anonymized summary (aggregate rating, flavor tag breakdown, all comments) at end of night. |
| 6 | **Leaderboard spoilers** | Show live top 5 during voting. Full results held for the admin-triggered reveal. |

---

## 4. App Sections

### 4.1 Planning Dashboard (Admin Only)

The command center. Only the admin sees this.

**Event Setup:**
- Event name, date, location, capacity (~100), max soups (~25)
- Voting config: ranking depth (default 5), tasting notes on/off

**Timeline Builder:**
Key milestones with status indicators (not started / in progress / done):
- Save the Date sent
- Soup sign-up opens / closes
- Formal invite sent
- Voting opens / closes
- Results revealed

**Soup Roster:**
- Table: soup name, cook, dietary tags, crockpot number, status (confirmed / tentative / withdrawn)
- Assign crockpot numbers (drag-to-reorder or manual entry)
- Export roster for day-of printing (table labels, etc.)

**Guest List:**
- RSVP tracker: invited, confirmed, declined, no response
- Manual add/import (CSV or one-by-one)
- Each guest has: name, email, phone (optional), invite channel preference (email / SMS / both / mail)
- Auto-generates unique link tokens

**Budget Tracker:**
- Simple line items with category (supplies, printing, food, venue, other)
- Estimated vs. actual cost per item
- Running total

**Day-of Checklist Generator:**
- Auto-generates based on soup count and guest count
- Items like: table layout, crockpot labels, napkins/bowls/spoons count, voting station QR codes, projector for leaderboard, extension cords
- Checkboxes — mark off as you go

**Campaign Manager:**
- See Section 4.3

---

### 4.2 Soup Sign-Up & Idea Generator

**Sign-Up Flow:**
1. Cook lands on `/signup` (linked from invite or shared URL)
2. Enters: name, soup name, short description (2-3 sentences), dietary tags (multi-select: vegan, vegetarian, gluten-free, dairy-free, contains nuts, spicy)
3. Optional: "surprise entry" flag — hides soup name from public until reveal
4. Submits → receives confirmation page with their unique cook link (to edit later)
5. Admin sees the new entry in the soup roster immediately

**Idea Generator** (`/ideas` — "What Should I Make?"):

Two modes, available to anyone:

**Spin the Wheel:**
- Animated wheel with soup categories: Classic Comfort, Around the World, Vegan/Plant-Based, Weird & Wonderful, Crowd Pleasers
- Spin → lands on a category → shows a random soup idea from that category
- Tap to spin again
- Each idea shows: name, one-line description, dietary info, effort level

**Prompt Mode:**
- Answer 3 questions:
  1. Vegan or anything goes?
  2. Adventurous or crowd-pleaser?
  3. How much effort? (Crockpot & forget / Some love / Showstopper)
- Returns 3 tailored soup suggestions with descriptions
- "Not feeling these? Try again" button for 3 more

**Data source:** Static JSON file of ~100 curated soup ideas with tags (category, vegan, effort, description). No AI generation — just a well-curated list.

---

### 4.3 Invitations & Drip Campaign

**Dual-Channel Invites:**

*Snail Mail:*
- App generates exportable content for the postcard: event name, date, location, tagline, QR code (links to shared RSVP page)
- Jay designs the final postcard in Canva using this content
- QR code is a shared link — when scanned, guest enters their name to RSVP, and the app creates/matches their guest record

*Electronic:*
- Email and/or SMS with a personalized unique link
- Same content as the postcard but with a direct link (no QR needed)

**Drip Campaign Timeline** (default template, fully editable by admin):

| Timing | Message | Channel |
|---|---|---|
| 6 weeks out | **Save the Date** — "The Great Soup Feast returns. Mark your calendar." | Email + Postcard |
| 4 weeks out | **Soup Sign-Up Opens** — "Think you've got what it takes? Register your soup." | Email + SMS |
| 2 weeks out | **The Lineup Teaser** — "25 soups confirmed. Here's a sneak peek at what's brewing." | Email + SMS |
| 1 week out | **Final Call** — "RSVP now. Voting goes live at the event." | Email + SMS |
| Day of | **Game Day** — "It's here. Scan, taste, vote." + voting link | SMS |
| Day after | **Results Are In** — "And the winner is... [link to reveal page]" | Email + SMS |

**Campaign Manager (admin):**
- Each message: template with merge fields (`{{guest_name}}`, `{{event_date}}`, `{{rsvp_link}}`, `{{vote_link}}`)
- Status per message: draft → scheduled → sent
- Send manually or schedule auto-send
- Track: sent count, opened (email only), clicked
- Test send to admin before blasting

---

### 4.4 Day-of Voting

**Flow:**
1. Guest scans QR code at the event OR opens their link from the SMS
2. Mobile-first voting page — no app install, no login, no friction
3. Sees all 25 soups: crockpot number, name, cook name, dietary tags
4. Ranks their **top 5** by dragging or tapping to add to ranking
5. Submits → fun confirmation ("Your taste buds have spoken!")
6. One ballot per unique token. Can update until admin closes voting.

**Voting UX:**
- Big, tappable soup cards optimized for phone screens
- Drag-to-rank OR tap-to-add-to-ranking (both interaction styles)
- Clear "your current ranking" section (sticky footer or sidebar)
- Dietary filter toggles: show only vegan, GF, etc.
- "I haven't tried this one yet" toggle per soup (optional — helps admin understand tasting coverage)
- Confirmation screen with option to revise before final submit

**Ranked Choice Voting (RCV) — Instant Runoff:**
- Round 1: Count all #1 votes. If any soup has >50% of first-place votes → winner.
- Round 2+: Eliminate the soup with the fewest first-place votes. Redistribute those ballots to each voter's next-ranked remaining soup. Repeat.
- Tiebreaker for elimination: if two soups tie for fewest votes, eliminate the one with fewer second-place votes. If still tied, eliminate the one registered later (arbitrary but deterministic).

**Computed Awards:**
- **Champion**: RCV winner
- **Runner-Up**: Last soup eliminated (2nd place in RCV)
- **Sleeper Hit**: Biggest positive jump — soup that gained the most vote share from Round 1 to its final round
- **Most Polarizing**: Highest standard deviation in ranking position across all ballots
- **Crowd Comfort**: Most combined "hearty" + "savory" tasting note tags (if tasting notes active)
- **Boldest Bowl**: Most combined "spicy" + "smoky" + "umami" tags (if tasting notes active)

---

### 4.5 Digital Tasting Notes (Experimental — Year 3 Test)

Low-friction, optional. Testing this year to gauge engagement. Kill it if nobody uses it.

**Per-Soup Mini Review:**
- Rating: 1-5 ladles (not stars — on brand)
- One-tap flavor tags: Savory, Sweet, Spicy, Creamy, Hearty, Light, Umami, Smoky
- Optional free-text note (max 140 chars)
- Default: private (not shared publicly)

**Where It Lives:**
- Accessible from the voting page — "add notes" link on each soup card
- Can be filled out before, during, or after voting (independent of ballot)
- Notes are private to the guest by default

**Cook Summary (End of Night):**
Each cook receives (via their cook link) a comprehensive feedback page:
- Aggregate ladle rating (average + distribution)
- Flavor tag breakdown (bar chart or word cloud)
- All anonymized guest comments
- How their soup ranked overall
- This page is only visible after results are revealed

**Admin View:**
- Aggregate ratings and tag clouds per soup
- Engagement stats: how many guests left notes, average notes per guest
- Data to decide whether to keep the feature next year

---

### 4.6 Live Leaderboard

Displayed on a big screen at the venue AND accessible on phones. The heartbeat of the event.

**Content Blocks (rotate every 10-15 seconds):**

| Block | What It Shows |
|---|---|
| **Vote Pulse** | Current vote count + animated progress bar. "78 of ~100 ballots cast!" |
| **Top 5** | Current top 5 soups by first-place votes. Updates live. (This is the spoiler — builds tension.) |
| **Soup Spotlight** | Random soup highlight — number, name, cook, fun description snippet |
| **Soup Fact** | Random soup trivia from a curated list. "The world's most expensive soup costs $5,000 and contains sea cucumber." |
| **Weird Stat** | Fun computed stat from live vote data. "Soup #7: ranked #1 or dead last — nothing in between." |
| **Dietary Breakdown** | Visual: how many entries are vegan / vegetarian / GF / etc. |
| **Voter Milestone** | Celebration when milestones hit: 25 votes, 50 votes, 75 votes, etc. |
| **Countdown** | Time remaining until voting closes. |

**Presentation Mode** (`/leaderboard?mode=tv`):
- Full-screen, auto-rotating, no interaction needed
- Dark background, large type, smooth animated transitions
- Designed for TV/projector — cast it and walk away

**Phone Mode** (`/leaderboard`):
- Same content blocks but scrollable
- Tap a soup to see details or leave tasting notes
- Pull-to-refresh for latest data

---

### 4.7 Results Reveal

Hidden until admin triggers it. This is the ceremony.

**Admin Trigger:**
- Admin goes to `/admin/reveal` → clicks "Reveal Results"
- Results page (`/results`) goes live for everyone
- All guest links now show a "Results are in!" banner linking to `/results`
- Day-after campaign message auto-sends (if scheduled)

**Reveal Page Flow:**

1. **Sankey Diagram** — the centerpiece
   - Animated visualization of RCV vote flow
   - Left: all 25 soups with Round 1 vote counts
   - Each elimination round shows votes redistributing to next-ranked soups
   - Eliminated soups fade out, surviving soups grow
   - Right: winner highlighted with celebration animation
   - Color-coded per soup — consistent colors throughout

2. **Final Rankings** — Full 1st through 25th ordered list

3. **Awards** — Champion, Runner-Up, Sleeper Hit, Most Polarizing, Crowd Comfort, Boldest Bowl (last two only if tasting notes were used)

4. **Soup Stories** (if tasting notes active) — Per-soup card: aggregate ladle rating, top flavor tags, highlighted guest notes

5. **Fun Stats Recap:**
   - Total votes cast
   - Average soups ranked per guest (did people rank all 5?)
   - Most-ranked soup (appeared on the most ballots)
   - Hidden Gem (fewest ballots — people missed it)
   - Voting time distribution (early birds vs. last-minute voters)
   - First vote timestamp, last vote timestamp

6. **"Your Ballot vs. Results"** (`/results/my/:token`)
   - Personal comparison: "You ranked X first — they finished 3rd!"
   - Shareable result card (generated image) for social media
   - Variations: "I voted for the winner!", "My #1 was the Sleeper Hit!", "I found the Hidden Gem!"

---

### 4.8 Archive

A public section showing the history of the Soup Feast.

**Per-Year Card:**
- Year, date, location
- Champion soup + cook
- Runner-Up + cook
- Total soups entered, total guests
- Full soup list (expandable)
- Photos (if uploaded by admin)
- Fun stats from that year (if available)

**Years 1 & 2:**
- Backfilled manually by admin
- Even partial data is fine — winner and soup list at minimum
- Builds the lore and tradition of the event

**Current Year:**
- Appears in the archive after results are revealed
- Auto-populated from the event data

---

## 5. Data Model

```
Event
  ├── id
  ├── name ("The Great Soup Feast 2026")
  ├── year (2026)
  ├── date
  ├── location
  ├── capacity (100)
  ├── voting_open (boolean)
  ├── results_revealed (boolean)
  ├── config
  │     ├── max_soups (25)
  │     ├── ranking_depth (5)
  │     └── tasting_notes_enabled (boolean)
  │
  ├── Soups[]
  │     ├── id
  │     ├── event_id
  │     ├── number (crockpot/table number, assigned by admin)
  │     ├── name
  │     ├── cook_name
  │     ├── cook_email (for sending their feedback summary)
  │     ├── description
  │     ├── dietary_tags[] (vegan, vegetarian, gf, df, nuts, spicy)
  │     ├── surprise_entry (boolean)
  │     ├── status (confirmed / tentative / withdrawn)
  │     └── created_at
  │
  ├── Guests[]
  │     ├── id
  │     ├── event_id
  │     ├── name
  │     ├── email (nullable)
  │     ├── phone (nullable)
  │     ├── invite_channel (email / sms / both / mail)
  │     ├── rsvp_status (invited / confirmed / declined / no_response)
  │     ├── unique_token
  │     ├── is_cook (boolean)
  │     ├── soup_id (nullable, FK → Soup if is_cook)
  │     └── created_at
  │
  ├── Ballots[]
  │     ├── id
  │     ├── guest_id
  │     ├── event_id
  │     ├── rankings[] (ordered array of soup_ids — position = rank)
  │     ├── submitted_at
  │     └── updated_at
  │
  ├── TastingNotes[]
  │     ├── id
  │     ├── guest_id
  │     ├── soup_id
  │     ├── event_id
  │     ├── rating (1-5)
  │     ├── flavor_tags[] (savory, sweet, spicy, creamy, hearty, light, umami, smoky)
  │     ├── note_text (max 140 chars)
  │     └── created_at
  │
  ├── Campaigns[]
  │     ├── id
  │     ├── event_id
  │     ├── name
  │     ├── send_date
  │     ├── channel (email / sms / both)
  │     ├── template_body (with merge fields)
  │     ├── status (draft / scheduled / sent)
  │     └── stats { sent, opened, clicked }
  │
  ├── BudgetItems[]
  │     ├── id
  │     ├── event_id
  │     ├── item_name
  │     ├── estimated_cost
  │     ├── actual_cost
  │     └── category (supplies / printing / food / venue / other)
  │
  └── ArchiveEvents[] (for years 1 & 2 backfill)
        ├── id
        ├── year
        ├── date
        ├── location
        ├── champion_soup
        ├── champion_cook
        ├── runner_up_soup
        ├── runner_up_cook
        ├── total_soups
        ├── total_guests
        ├── soup_list[] (names only)
        └── photos[] (URLs)
```

Note: `ArchiveEvents` is a simplified model for backfilling years 1 & 2. Year 3+ uses the full `Event` model and auto-archives when results are revealed.

---

## 6. Tech Stack

| Layer | Choice | Why |
|---|---|---|
| **Framework** | Next.js 14+ (App Router) | Full-stack in one project. SSR for reveal, API routes for voting, static for leaderboard. |
| **Database** | PostgreSQL via Supabase | Relational data, real-time subscriptions built in, generous free tier, auth for admin. |
| **ORM** | Prisma | Type-safe, great DX, good migration story. |
| **Admin Auth** | Supabase Auth (email/password) | Just for admin. One account. |
| **Guest Auth** | Token-based links | No login. Unique URL token per guest. |
| **Real-time** | Supabase Realtime | Live leaderboard updates as votes land. Already included with Supabase. |
| **Email** | Resend | Simple API, generous free tier (100 emails/day free — plenty for this). |
| **SMS** | Twilio | ~$0.008/msg. Budget: ~$5/event for 600 messages. |
| **Sankey Diagram** | D3.js | Full control over animation and interactivity for the RCV flow visualization. |
| **Hosting** | Vercel | Free tier. Auto-deploys from GitHub. Native Next.js support. |
| **Styling** | Tailwind CSS | Fast to build, easy to theme. |
| **QR Codes** | `qrcode` npm package | Generate QR codes for postcards and event-day voting stations. |

---

## 7. Soup Idea Generator — Data

Static JSON file, ~100 curated soups:

```json
{
  "soups": [
    {
      "name": "Roasted Red Pepper & Gouda",
      "category": "crowd_pleaser",
      "vegan": false,
      "effort": "some_love",
      "description": "Smoky roasted peppers with melty gouda. Dangerously good."
    },
    {
      "name": "West African Peanut Stew",
      "category": "around_the_world",
      "vegan": true,
      "effort": "some_love",
      "description": "Rich, spicy, peanutty. Sweet potato and greens. A showstopper."
    },
    {
      "name": "Miso Mushroom Ramen Broth",
      "category": "around_the_world",
      "vegan": true,
      "effort": "showstopper",
      "description": "Deep umami broth with shiitake, kombu, and a hit of ginger."
    }
  ]
}
```

**Categories:** `classic_comfort`, `around_the_world`, `vegan_plant_based`, `weird_wonderful`, `crowd_pleaser`

**Effort levels:** `crockpot_and_forget`, `some_love`, `showstopper`

Full list of ~100 soups to be curated during build — balanced across categories and dietary options.

---

## 8. Page Map

```
PUBLIC PAGES
/                              → Landing page: event info, countdown, RSVP link, archive link
/rsvp/:token                   → Guest RSVP page (personalized via token)
/rsvp                          → Shared RSVP page (from postcard QR — guest enters name)
/signup                        → Cook soup registration form
/ideas                         → Soup idea generator (spin the wheel + prompt mode)
/vote/:token                   → Voting page (mobile-first, ranked choice)
/leaderboard                   → Live leaderboard (phone mode — scrollable)
/leaderboard?mode=tv           → Live leaderboard (TV/projector — full-screen, auto-rotating)
/results                       → Results reveal (hidden until admin triggers)
/results/my/:token             → "Your ballot vs. results" personal comparison
/archive                       → Past years: winners, soup lists, photos

ADMIN PAGES (password protected)
/admin                         → Planning dashboard overview
/admin/event                   → Event setup & config
/admin/timeline                → Timeline builder with milestone statuses
/admin/soups                   → Soup roster management
/admin/guests                  → Guest list & RSVP tracking
/admin/campaigns               → Campaign manager (drip messages)
/admin/budget                  → Budget tracker
/admin/checklist               → Day-of checklist
/admin/reveal                  → Trigger results reveal
/admin/archive                 → Manage archive entries (backfill years 1 & 2)
```

---

## 9. RCV Algorithm

```
function runRCV(ballots, soups):
    rounds = []
    remaining = [...soups]

    while remaining.length > 1:
        // Count first-place votes among remaining soups
        counts = {}
        for each remaining soup: counts[soup] = 0

        for ballot in ballots:
            topPick = first item in ballot.rankings that is in remaining
            if topPick: counts[topPick] += 1

        totalVotes = sum of all counts
        round = { counts: {...counts}, eliminated: null, redistributed: {} }

        // Check for majority winner
        for soup, count in counts:
            if count > totalVotes / 2:
                rounds.push(round)
                return { winner: soup, rounds, finalRankings: buildRankings(rounds) }

        // Find soup to eliminate (fewest first-place votes)
        minCount = min of counts values
        candidates = soups where counts[soup] == minCount

        // Tiebreaker: fewest second-place votes, then latest registration
        eliminated = breakTie(candidates, ballots, remaining)

        // Track where eliminated soup's votes go
        redistributed = {}
        for ballot in ballots:
            if first remaining pick == eliminated:
                nextPick = second remaining pick in ballot.rankings
                if nextPick: redistributed[nextPick] = (redistributed[nextPick] || 0) + 1

        round.eliminated = eliminated
        round.redistributed = redistributed
        remaining.remove(eliminated)
        rounds.push(round)

    return { winner: remaining[0], rounds, finalRankings: buildRankings(rounds) }
```

The `redistributed` map in each round is what powers the Sankey diagram — it shows exactly where each eliminated soup's votes went.

---

## 10. Build Order

Recommended sequence — each phase is deployable independently:

| Phase | What | Why First |
|---|---|---|
| **1** | Data model + DB schema + admin auth | Foundation. Nothing works without data. |
| **2** | Admin dashboard: event setup, soup roster, guest list | You need the planning tools weeks before the event. |
| **3** | Soup sign-up + idea generator | Open registration early. Gets cooks excited. |
| **4** | Invite system: QR code export, RSVP pages, Canva content | Send save-the-dates 6 weeks out. |
| **5** | Campaign manager + Resend/Twilio integration | Drip messages need to be scheduled. |
| **6** | Budget tracker + day-of checklist | Admin quality-of-life before the event. |
| **7** | Voting system + RCV engine | Core game-day feature. Test heavily. |
| **8** | Live leaderboard (TV + phone modes) | Game-day display. Depends on voting data. |
| **9** | Results reveal + Sankey diagram + awards | Post-event ceremony. Build after voting is solid. |
| **10** | Tasting notes | Experimental. Build last. Layer onto existing soup cards. |
| **11** | Archive section | Backfill years 1 & 2. Can happen anytime. |
| **12** | Shareable result cards | Nice-to-have. Post-event social sharing. |

---

## 11. Non-Functional Requirements

| Requirement | Target |
|---|---|
| **Mobile-first** | Voting, leaderboard, and results pages must be fully usable on a phone. Desktop is secondary. |
| **No login for guests** | Zero friction. Token-based links only. |
| **Performance** | Voting page loads in <2s on 4G. Leaderboard updates within 3s of a vote. |
| **Offline resilience** | If a guest loses signal mid-vote, the ballot should be cached locally and submitted when connection returns. |
| **Capacity** | 100 concurrent voters. Not a scale challenge — but the real-time leaderboard needs to handle rapid updates gracefully. |
| **Data privacy** | No personal data beyond name + contact info. Tasting notes anonymized for cook summaries. Guest data deleted 90 days post-event (or kept for archive with consent). |
| **Accessibility** | Voting page should be usable with screen readers. Color is not the only indicator in the leaderboard. |
